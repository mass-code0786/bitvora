import { Prisma } from "@prisma/client";
export type SalaryInvariantRow={status:"PENDING"|"PAID"|"SKIPPED"|"FAILED";amount:string|number|Prisma.Decimal;ledgerAmount:string|number|Prisma.Decimal|null;outboxCount:number};
export function checkSalaryInvariants(rows:readonly SalaryInvariantRow[]){const paid=rows.filter(row=>row.status==="PAID"),paidAmount=paid.reduce((sum,row)=>sum.add(row.amount),new Prisma.Decimal(0)),ledgerAmount=paid.reduce((sum,row)=>sum.add(row.ledgerAmount??0),new Prisma.Decimal(0)),paidMissingLedger=paid.filter(row=>row.ledgerAmount===null).length,skippedWithLedger=rows.filter(row=>row.status==="SKIPPED"&&row.ledgerAmount!==null).length,duplicateNotifications=rows.filter(row=>row.outboxCount>1).length;return{paidAmount,ledgerAmount,paidMissingLedger,skippedWithLedger,duplicateNotifications,valid:paidAmount.eq(ledgerAmount)&&paidMissingLedger===0&&skippedWithLedger===0&&duplicateNotifications===0}}
