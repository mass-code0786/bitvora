"use client";
import { Network, Search, UserCheck, UserMinus, UserPlus, UsersRound, WalletCards } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useWalletStore } from "@/hooks/use-wallet-store";
import { formatCurrency } from "@/lib/currency";

type Member={id:string;uid:string;name:string;sponsorId:string;sponsorUid:string;joinDate:string;level:number};
export function TeamModule(){const[members,setMembers]=useState<Member[]>([]),[query,setQuery]=useState(""),{store}=useWalletStore();useEffect(()=>{void fetch("/api/team",{cache:"no-store"}).then(response=>response.ok?response.json():{records:[]}).then(data=>setMembers(data.records??[]))},[]);const visible=useMemo(()=>{const text=query.trim().toLowerCase();return members.filter(member=>!text||member.name.toLowerCase().includes(text)||member.uid.toLowerCase().includes(text))},[members,query]),direct=members.filter(member=>member.level===1).length,newThisMonth=members.filter(member=>{const date=new Date(member.joinDate),now=new Date();return date.getFullYear()===now.getFullYear()&&date.getMonth()===now.getMonth()}).length,business=store.rankAccount.qualifyingFutureCapital;return <div className="team-module"><header className="rt-page-header"><div><span>Five-level network</span><h1>Team</h1><p>Explore your organisation and business.</p></div></header><section className="team-overview"><div><span><Network size={19}/></span><div><small>Total network</small><strong>{members.length}</strong><p>Members across five levels</p></div></div><div className="team-overview-grid"><article><UsersRound size={15}/><span>Direct team</span><strong>{direct}</strong></article><article><UserCheck size={15}/><span>Qualified team</span><strong>{store.rankAccount.qualifiedTeamCount}</strong></article><article><UserMinus size={15}/><span>Unqualified team</span><strong>{Math.max(0,members.length-store.rankAccount.qualifiedTeamCount)}</strong></article><article><UserPlus size={15}/><span>New this month</span><strong>{newThisMonth}</strong></article><article className="wide"><WalletCards size={15}/><span>Qualifying business</span><strong>{formatCurrency(business)}</strong></article></div></section><section><div className="rt-section-title"><div><span>Network distribution</span><h2>Level breakdown</h2></div><b>{members.length} members</b></div><div className="level-breakdown">{[1,2,3,4,5].map(level=>{const count=members.filter(member=>member.level===level).length,share=members.length?Math.round(count/members.length*100):0;return <article key={level}><div className="level-index">L{level}</div><div className="level-copy"><div><strong>Level {level}</strong><span>{share}% of network</span></div><div className="level-bar"><i style={{width:`${share}%`}}/></div><dl><div><dt>Members</dt><dd>{count}</dd></div></dl></div></article>})}</div></section><section><div className="rt-section-title"><div><span>Member directory</span><h2>Search team</h2></div><b>{visible.length} results</b></div><div className="team-search"><label><Search size={16}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search name or user ID"/></label></div><div className="team-directory">{visible.length?visible.map(member=><article key={member.id}><span className="rt-avatar"><b>{member.name.split(/\s+/).slice(0,2).map(value=>value[0]).join("")}</b></span><button><strong>{member.name}</strong><span>{member.uid} · Level {member.level}</span><small>Joined {new Date(member.joinDate).toLocaleDateString()}</small></button></article>):<div className="team-empty"><Search size={20}/><strong>No members found</strong><span>Team members will appear here.</span></div>}</div></section></div>}
