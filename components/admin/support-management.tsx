"use client";
import { useCallback, useEffect, useState } from "react";
type User={id:string;uid:string;name:string;email:string;supportRestrictions:{banned:boolean;reason:string|null}|null};
type Reply={id:string;message:string;createdAt:string};
type Ticket={id:string;subject:string;category:string;priority:string;message:string;status:string;adminReply:string|null;updatedAt:string;hasAttachment:boolean;user:User;replies:Reply[]};
type Conversation={id:string;title:string;lastMessageAt:string;user:User;messages:Array<{id:string;role:string;message:string;createdAt:string}>};
const empty={supportEmail:"",whatsappUrl:"",telegramUrl:"",helpCenterUrl:""};
export function SupportManagement(){const[tickets,setTickets]=useState<Ticket[]>([]),[conversations,setConversations]=useState<Conversation[]>([]),[configuration,setConfiguration]=useState(empty),[message,setMessage]=useState(""),[tab,setTab]=useState<"tickets"|"chats">("tickets"),[drafts,setDrafts]=useState<Record<string,string>>({});const load=useCallback(()=>void fetch("/api/admin/support",{cache:"no-store"}).then(async response=>{const data=await response.json();if(response.ok){setTickets(data.tickets);setConversations(data.conversations);setConfiguration({...empty,...data.configuration})}}),[]);useEffect(load,[load]);const call=async(body:object)=>{const response=await fetch("/api/admin/support",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),data=await response.json();setMessage(response.ok?data.message:data.error);if(response.ok)load()};const saveConfiguration=async(event:React.FormEvent)=>{event.preventDefault();const response=await fetch("/api/admin/support",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(configuration)}),data=await response.json();setMessage(response.ok?data.message:data.error)};const setStatus=(id:string,status:string)=>setTickets(current=>current.map(ticket=>ticket.id===id?{...ticket,status}:ticket));return <div className="admin-page"><header className="admin-page-head"><div><small>Customer care</small><h1>AI Support</h1></div></header>{message&&<div className="admin-banner">{message}</div>}<form className="admin-form admin-support-config" onSubmit={saveConfiguration}><h2>Official support channels</h2><input placeholder="Support email (optional)" value={configuration.supportEmail} onChange={event=>setConfiguration({...configuration,supportEmail:event.target.value})}/><input placeholder="Help Center URL (optional)" value={configuration.helpCenterUrl} onChange={event=>setConfiguration({...configuration,helpCenterUrl:event.target.value})}/><input placeholder="WhatsApp URL (optional)" value={configuration.whatsappUrl} onChange={event=>setConfiguration({...configuration,whatsappUrl:event.target.value})}/><input placeholder="Telegram URL (optional)" value={configuration.telegramUrl} onChange={event=>setConfiguration({...configuration,telegramUrl:event.target.value})}/><button>Save support settings</button></form><div className="admin-tabs"><button className={tab==="tickets"?"active":""} onClick={()=>setTab("tickets")}>Tickets ({tickets.length})</button><button className={tab==="chats"?"active":""} onClick={()=>setTab("chats")}>AI conversations ({conversations.length})</button></div>{tab==="tickets"?<section className="admin-support-tickets">{tickets.map(ticket=><article className="admin-card" key={ticket.id}><header><div><strong>{ticket.subject}</strong><small>{ticket.user.name} · {ticket.user.uid} · {ticket.user.email}</small></div><select value={ticket.status} onChange={event=>setStatus(ticket.id,event.target.value)}>{["OPEN","IN_PROGRESS","RESOLVED","CLOSED"].map(status=><option key={status}>{status}</option>)}</select></header><p><b>{ticket.category} · {ticket.priority}</b><br/>{ticket.message}</p>{ticket.hasAttachment&&<a href={`/api/support/attachment/${ticket.id}`} target="_blank">View secure attachment</a>}{ticket.replies.map(reply=><aside key={reply.id}><b>Admin reply</b><p>{reply.message}</p></aside>)}<textarea rows={4} maxLength={4000} placeholder="Reply to this ticket" value={drafts[ticket.id]??""} onChange={event=>setDrafts({...drafts,[ticket.id]:event.target.value})}/><div className="admin-support-actions"><button onClick={()=>void call({action:"STATUS",ticketId:ticket.id,status:ticket.status})}>Save status</button><button disabled={!drafts[ticket.id]?.trim()} onClick={()=>void call({action:"REPLY",ticketId:ticket.id,status:ticket.status,message:drafts[ticket.id]})}>Send reply</button></div></article>)}</section>:<section className="admin-support-conversations">{conversations.map(chat=><article className="admin-card" key={chat.id}><header><div><strong>{chat.title}</strong><small>{chat.user.name} · {chat.user.uid} · {new Date(chat.lastMessageAt).toLocaleString()}</small></div><button className={chat.user.supportRestrictions?.banned?"danger active":"danger"} onClick={()=>void call({action:"BAN",userId:chat.user.id,banned:!chat.user.supportRestrictions?.banned,reason:chat.user.supportRestrictions?.banned?"":"Restricted by support administrator"})}>{chat.user.supportRestrictions?.banned?"Restore AI chat":"Restrict AI chat"}</button></header><div className="admin-chat-transcript">{chat.messages.map(item=><p className={item.role.toLowerCase()} key={item.id}><b>{item.role}</b>{item.message}</p>)}</div></article>)}</section>}</div>}
