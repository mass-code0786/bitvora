"use client";
import { motion } from "framer-motion";
import { Sparkline } from "./charts";
import { CryptoCoin } from "./crypto-coin";
import { formatCurrency } from "@/lib/currency";
export function MarketCard({coin}:{coin:{symbol:string;name:string;price:number;change:string;volume:number;color:string;chart:number[]}}){const isCoin=["BTC","ETH","BNB"].includes(coin.symbol);return <motion.article whileHover={{y:-5,scale:1.012}} whileTap={{scale:.985}} transition={{type:"spring",stiffness:320,damping:24}} className="glass-card market-card min-w-[166px] flex-1 p-4"><div className="flex items-center justify-between">{isCoin?<CryptoCoin symbol={coin.symbol as "BTC"|"ETH"|"BNB"} size="sm"/>:<span className="coin-icon premium-coin-icon" style={{color:coin.color}}>{coin.symbol[0]}</span>}<span className="market-change">{coin.change}</span></div><div className="market-chart mt-4 h-14"><Sparkline data={coin.chart} color={coin.color}/></div><div className="mt-3 flex items-end justify-between gap-2"><div><p className="text-sm font-semibold text-white">{coin.symbol}</p><p className="mt-1 text-sm font-medium tracking-tight text-slate-200">{formatCurrency(coin.price,{maximumFractionDigits:4})}</p></div></div><div className="mt-3 flex items-center justify-between border-t border-white/[.05] pt-2.5 text-[9px]"><span className="text-slate-600">24h volume</span><span className="text-slate-400">{formatCurrency(coin.volume,{notation:"compact",maximumFractionDigits:1})}</span></div></motion.article>}
