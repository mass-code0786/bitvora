import {describe,expect,it} from "vitest";
import {readFileSync} from "node:fs";
import {COUNTRY_OPTIONS} from "@/lib/countries";
describe("registration country picker",()=>{const form=readFileSync("components/auth-form.tsx","utf8");it("starts without a selected country and has no IP detection",()=>{expect(form).toContain('useState<CountryOption|null>(null)');expect(form).not.toMatch(/geo|geolocation|ipapi|ipinfo/i)});it("uses the searchable picker instead of a native select",()=>{expect(form).toContain("<CountryPicker");expect(form).not.toContain("<select")});it("provides a complete searchable ISO catalogue",()=>{expect(COUNTRY_OPTIONS.length).toBeGreaterThan(200);expect(COUNTRY_OPTIONS).toEqual(expect.arrayContaining([expect.objectContaining({code:"IN",name:"India"}),expect.objectContaining({code:"AE",name:"United Arab Emirates"}),expect.objectContaining({code:"US",name:"United States"}),expect.objectContaining({code:"BD",name:"Bangladesh"}),expect.objectContaining({code:"PK",name:"Pakistan"})]))})});
