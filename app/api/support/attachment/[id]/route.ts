import { NextResponse } from "next/server";
import { requireAuthenticatedUser } from "@/lib/auth/server";
import { prisma } from "@/lib/prisma";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireAuthenticatedUser(),{id}=await params,ticket=await prisma.supportTicket.findFirst({where:{id,...(user.role==="ADMIN"?{}:{userId:user.id})},select:{userId:true,attachmentData:true,attachmentMimeType:true,attachmentName:true}});if(!ticket?.attachmentData)return NextResponse.json({error:"Attachment not found."},{status:404});if(user.role==="ADMIN")await prisma.supportAuditLog.create({data:{adminUserId:user.id,targetUserId:ticket.userId,ticketId:id,action:"ATTACHMENT_ACCESSED",metadata:{filename:ticket.attachmentName,mimeType:ticket.attachmentMimeType}}});return new Response(ticket.attachmentData,{headers:{"content-type":ticket.attachmentMimeType??"application/octet-stream","content-disposition":`attachment; filename="${(ticket.attachmentName??"attachment").replace(/["\r\n]/g,"")}"`,"cache-control":"private, no-store","content-security-policy":"sandbox","x-content-type-options":"nosniff"}})}catch{return NextResponse.json({error:"Authentication required."},{status:401})}}
